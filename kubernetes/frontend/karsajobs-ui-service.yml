apiVersion: v1
kind: Service
metadata:
  name: karsajobs-ui-service
spec:
  selector:
    app: karsajobs-frontend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: NodePort
